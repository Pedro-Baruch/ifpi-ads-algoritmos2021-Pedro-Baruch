// Q9 - Leia 2 números (A, B) e escreva-os em ordem inversa (B, A)

const input = require('prompt-sync')()

// Entrada
const A = Number(input('Numero A: '))
const B = Number(input('Numero B: '))

// Processamento


// Saída
console.log('---', A, B,'---', B, A)